<!--pages/function/function.wxml-->
<view wx:for="{{functionArray}}" class='button' data-index='{{index}}' bindtap='bindFunction'>
    <image src='{{item.url}}'></image>
    <view>{{item.name}}</view>
</view>
